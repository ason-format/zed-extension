[package]
name = "ason-zed-extension"
version = "1.0.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/ason-format/zed-extension"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.2.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
